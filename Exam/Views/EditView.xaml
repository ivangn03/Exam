<Window x:Class="Exam.Views.EditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Exam.Views"
        mc:Ignorable="d"
        Title="EditView" Height="450" Width="800"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        >
    <Grid>
        <StackPanel>
            <Label Content="{DynamicResource Framework}"/>
            <TextBox Margin="20,0,0,0" Text="{Binding Framework.Name}"></TextBox>
            <Label Content="{DynamicResource Language}"/>
            <TextBox Margin="20,0,0,0" Text="{Binding Framework.Language}"></TextBox>
            <Label Content="{DynamicResource ImageURL}"/>
            <TextBox Margin="20,0,0,4" Text="{Binding Framework.ImageURL}"></TextBox>
            <Button Command="{Binding BackToMain}" Background="#039be5">Return</Button>
        </StackPanel>
    </Grid>
</Window>
